version: '3.9'
services:
  backend:
    build:
      context: .
      dockerfile: deployment/Dockerfile
    ports:
      - '3001:3001'
    env_file:
      - .env.example
  frontend:
    image: nginx:alpine
    ports:
      - '3000:80'
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
